---
title: Theme & Colors
description: Theme switching and custom colors in Kaku
---

## Follow system

By default, Kaku follows the macOS light/dark appearance and switches automatically.

## Manual override

Force a specific theme via Lua — see [Lua Configuration](/Kaku/en/docs/config/lua/) for `config.color_scheme` and related options.
